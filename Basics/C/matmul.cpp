#include <iostream>
#include <vector>

int main()
{
    //-xHost -O3 -prec-div -no-ftz -restrict 
    // These directives enable aggressive optimizations, precise division, disable flush-to-zero, and allow pointer aliasing for better performance.

}